apiVersion: v2
name: "dm-kubernetes-dashboard"
description: "An umbrella chart for the kubernetes dashboard"
type: application
version: 0.1.0
appVersion: v3.0.0
dependencies:
  - name: kubernetes-dashboard
    version: 7.0.3
    repository: "https://kubernetes.github.io/dashboard"
